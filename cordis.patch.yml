# dsh-agent-preset-switcher bundle patch: insert the host plugin row.
# Host-only: switching is done with the /mode slash command inside a session.
- insert:
    - id: mode-switcher
      name: 'dsh-agent-preset-switcher'
